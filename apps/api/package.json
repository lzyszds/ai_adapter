{
  "name": "@llm-shield/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@llm-shield/shared": "workspace:*",
    "@elysiajs/cors": "^1.1.1",
    "elysia": "^1.1.20",
    "ioredis": "^5.4.1",
    "zod": "^3.23.8",
    "@prisma/client": "^6.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "prisma": "^6.2.1",
    "typescript": "^5.6.3"
  }
}
